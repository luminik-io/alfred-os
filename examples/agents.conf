# Example agents.conf for the pennyworth examples.
#
# Format documented in launchd/agents.conf.example. TSV: label, script,
# schedule, needs_java, optional log_stem.

my.fleet.hello	hello.py	interval:3600	no
