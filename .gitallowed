# Files allowed to contain regex patterns that look like secrets.
# These are the scrub-check script (which DEFINES the patterns) and CI
# configuration that may reference them. They are scanned at runtime by
# bin/scrub-check.sh itself; pre-commit git-secrets needs an allowlist
# entry so it doesn't double-flag pattern literals as actual secrets.
bin/scrub-check\.sh
# .gitleaks.toml DEFINES allowlist regexes that reference fake placeholder
# token prefixes (the ant-token setup-token examples); like scrub-check.sh
# it must be exempt so pre-commit git-secrets does not double-flag those
# pattern literals as real secrets.
\.gitleaks\.toml
\.github/workflows/.*\.yml
# Documentation prose and SQL schema files. The pre-commit git-secrets
# scan flagged short config-format examples and standard SQL constraints
# in these files as false positives; this allowlist exempts them.
\.alfredrc\.example
bin/agent-launch
bin/alfred-init\.py
lib/fleet_brain/schema\.py
systemd/render\.sh
# CLAUDE_CODE_OAUTH_TOKEN setup uses an obviously-fake placeholder
# value. The generic ant-token regex would otherwise flag the literal
# prefix that ``claude setup-token`` emits in: the docs example, the
# parser regex in ``bin/alfred-setup-token.py``, and the test fixtures.
docs/CLAUDE_CODE\.md
docs/AI_ASSISTED_INSTALL\.md
bin/alfred-setup-token\.py
tests/test_alfred_setup_token\.py
