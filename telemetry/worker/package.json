{
  "name": "alfred-proof-telemetry-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker for Alfred anonymous usage totals.",
  "license": "MIT",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  }
}
